import React from 'react';
import { View, Text, SafeAreaView, ScrollView } from 'react-native';

const CircleScreen = () => (
  <SafeAreaView style={{ flex: 1 }}>
    <ScrollView>
      <Text>circle Information</Text>
    </ScrollView>
  </SafeAreaView>
);

export default CircleScreen;
